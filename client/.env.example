# Backend API URL (Optional on Vercel: leave blank in production for same-origin serverless routing,
# or set to http://localhost:8000 for local development)
NEXT_PUBLIC_API_URL=http://localhost:8000

# Client App URL for OAuth redirects, share links, and domain references
NEXT_PUBLIC_APP_URL=http://localhost:3000
